<h1>All Venues</h1>
<% @venues.each do |venue| %>
  <li><%= link_to venue.name, venue_path(venue) %></li><br/>
<% end %>

<% if can? :create, @venue %><h4><%= link_to 'Add New Venue', "/venues/new" %> </h4><% end %>
