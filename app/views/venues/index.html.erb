<h1>All Venues</h1>
<% @venues.each do |venue| %>
  <li><%= link_to venue.name, venue_path(venue) %></li><br/>
<% end %>

<h4><% if can? :create, @venue %><%= link_to 'Add New Venue', "/venues/new" %><% end %> </h4>
