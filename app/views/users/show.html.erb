<h1><%= @user.name %></h1>
<h2>Contact</h2>
<%= @user.email %><br/>


<% if can? :update, @user %><%= link_to 'Edit Account', edit_user_path(@user) %><br/><% end %>
<% if can? :destroy, @user %><%= link_to 'Delete', @user, method: :delete, data: { confirm: 'For real?' }  %><% end %>
